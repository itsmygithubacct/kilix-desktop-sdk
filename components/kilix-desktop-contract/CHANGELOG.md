# Changelog

## 1.0.0-rc1

- Define the desktop-provider protocol v1 schemas and vocabularies.
- Freeze the existing `kilix_tui` and `kilix_desk` import identities and the
  installed command names supplied by `kilix-tui-utils`.
- Add dependency-light Python and C11 helpers, canonical fixtures, and a
  byte-bound two-pass self-test.
